def nueva_matriz(filas, columnas, valor):
    matriz = []
    for i in range(filas):
        matriz.append([valor] * columnas)
    return matriz

def promedio_entorno(imagen, fila, columna):


def difuminada(imagen, valor):
